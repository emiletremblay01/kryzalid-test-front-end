<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>Kryzalid Test</title>
        <link rel="stylesheet" href="./css/style.css">
    </head>
    <body>
        <div class="container">
            {% include 'partials/readme.twig' %}
        </div>

        <div class="news-container">
            {% for post in posts %}
                {% include 'partials/news-card.twig' with { post: post } %}
            {% endfor %}
        </div>

        {% include 'partials/footer.twig' %}

        <script src="./js/script.js"></script>
    </body>
</html>