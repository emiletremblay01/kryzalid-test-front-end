<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>Kryzalid Test</title>
        <link rel="stylesheet" href="./css/style.css">
    </head>
    <body>
        <div class="background-shape">
            <svg width="2284" height="1282" viewBox="0 0 2284 1282" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path opacity="0.5" d="M9.70803 1108.1C9.70803 1108.1 2955.47 301.094 2080.79 74.8425C1206.1 -151.409 1100.59 802.168 1878.88 1246.68" stroke="#D1D1D1" stroke-width="80" stroke-miterlimit="10"/>
            </svg>
        </div>
        <div class="container">
            <div style="display: flex; justify-content: space-between; align-items: center;">
                <h1>Nouvelles</h1>

                <div style="display: flex; align-items: center; gap: 1rem;">
                    <div>Afficher</div>
                    <div class="category-filter">
                        <select id="categorySelect" class="filter-select">
                            <option value="all">Toutes</option>
                            {% for categoryId in categoryIds %}
                                <option value="{{ categoryId }}">Catégorie {{ categoryId }}</option>
                            {% endfor %}
                        </select>
                    </div>
                </div>
            </div>

            <div class="news-container">
                {% for post in posts %}
                    {% include 'partials/news-card.twig' with { post: post } %}
                {% endfor %}
            </div>

            <!-- Pagination controls will be added here by JavaScript -->

        </div>

        {% include 'partials/footer.twig' %}

        <script src="./js/script.js"></script>
    </body>
</html>